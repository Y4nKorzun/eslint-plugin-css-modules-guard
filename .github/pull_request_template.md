## What and why

## Testing

- [ ] `npm run test:coverage` passes (100% line/branch/function coverage)
- [ ] `npm run pack:check` passes
- [ ] Added or updated fixtures under `test/fixtures/` for the new behavior
- [ ] Updated `README.md` if a rule, option, or CLI flag changed
- [ ] Added a `CHANGELOG.md` entry under `[Unreleased]`

## Minimal reproducer for bug fixes

If this fixes a bug, the case that used to fail (and now doesn't):
